"use client";

interface ThemeToggleProps {
  className?: string;
  variant?: "default" | "compact" | "sidebar";
}

// Dark mode only — theme toggle o'chirilgan
export function ThemeToggle({ className = "" }: ThemeToggleProps) {
  return null;
}
